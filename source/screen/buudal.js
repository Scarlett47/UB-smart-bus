import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function Buudal({ navigation }) {
  return (
    <View>
      <Text>buudal</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
