import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export default function Medee({ navigation }) {
  return (
    <View>
      <Text>medee</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
